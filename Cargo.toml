[profile.dev]
codegen-units = 4

[workspace]
members = [
  "core",
  "extra",
  "macros",
  "examples/hello_world",
  "examples/file_list",
  "examples/basic_auth",
]

[patch.crates-io]
salvo = { path = "core" }
salvo_macros = { path = "macros" }
salvo_extra = { path = "extra" }
