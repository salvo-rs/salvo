export const topicsChildren = function (bookRoot: string) {
  return [
    `${bookRoot}/topics/handle-error.md`,
    `${bookRoot}/topics/file-upload.md`,
    `${bookRoot}/topics/working-with-database.md`,
    `${bookRoot}/topics/use-template-engine.md`,
  ];
}