export const coreChildren = function (bookRoot: string) {
  return [
    `${bookRoot}/core/router.md`,
    `${bookRoot}/core/request.md`,
    `${bookRoot}/core/response.md`,
    `${bookRoot}/core/depot.md`,
    `${bookRoot}/core/handler.md`,
    `${bookRoot}/core/catcher.md`,
    `${bookRoot}/core/writer.md`,
    `${bookRoot}/core/testing.md`,
  ];
}