[package]
name = "file_list"
version = "0.0.0"
workspace = "../../"
edition = "2018"
publish = false

[dependencies]
hyper = { version = "0.14" }
tracing = "0.1"
tracing-subscriber = "0.2"
salvo_core = { path = "../../core" }
salvo_extra = { path = "../../extra" }
tokio = { version = "1.0", features = ["full"] }
